apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kafka-ui-chart.serviceAccountName" . }}
  namespace: {{ include "kafka-ui-chart.namespace" . }}
  labels:
    {{- include "kafka-ui-chart.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: true
