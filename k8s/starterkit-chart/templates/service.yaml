apiVersion: v1
kind: Service
metadata:
  name: {{ include "starterkit.fullname" . }}
  labels:
    {{- include "starterkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "starterkit.selectorLabels" . | nindent 4 }}
