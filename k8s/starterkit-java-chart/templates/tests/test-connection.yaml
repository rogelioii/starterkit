apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starterkit-java-chart.fullname" . }}-test-connection"
  labels:
    {{- include "starterkit-java-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starterkit-java-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
