apiVersion: v1
kind: Service
metadata:
  name: {{ include "starterkit-java-chart.fullname" . }}
  labels:
    {{- include "starterkit-java-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starterkit-java-chart.selectorLabels" . | nindent 4 }}
